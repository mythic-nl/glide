using DataTypes;
using UnityEngine;

public class UserInterface : MonoBehaviour
{
    // [SerializeField, Tooltip("Show the velocity vector in the UI")]
    // private VectorVariable velocity;
    //
    // [SerializeField, Tooltip("Show the max speed value in the UI")]
    // private FloatVariable maxSpeed;
    //
    // [SerializeField, Tooltip("Show the max acceleration value in the UI")]
    // private FloatVariable maxAcceleration;
    //
    // private void OnGUI()
    // {
    //     GUI.Label(new Rect(10, 10, 300, 20), $"Velocity: {velocity.value}");
    //     GUI.Label(new Rect(10, 30, 300, 20), $"Magnitude: {velocity.value.magnitude}");
    //     GUI.Label(new Rect(10, 50, 300, 20), $"Max Speed: {maxSpeed.value}");
    //     GUI.Label(new Rect(10, 70, 300, 20), $"Max Acceleration: {maxAcceleration.value}");
    // }
}